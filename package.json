{
  "name": "vivaan-dhawan",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/stats-listener.js --bundle --format=esm --minify --platform=browser --outfile=firebase-listener.js && esbuild src/admin-app.js --bundle --format=esm --minify --platform=browser --outfile=admin.js && esbuild src/smooth-scroll.js --bundle --format=esm --minify --platform=browser --outfile=smooth-scroll.js"
  },
  "dependencies": {
    "firebase": "10.10.0",
    "lenis": "1.3.15"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
